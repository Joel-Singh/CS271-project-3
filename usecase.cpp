#include "HashTable.h"

using namespace std;

template <typename T> 
HashTable<T>* create_table(string fname, int m) {}

template <typename T> 
bool login(HashTable<T>* ht, T username, string password) {}
