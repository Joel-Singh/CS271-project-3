#include "HashTable.h"

template <typename T> HashTable<T>::HashTable(int size) {}

template <typename T> void HashTable<T>::insert(T data, int key) {}

template <typename T> void HashTable<T>::remove(T data, int key) {}

template <typename T> bool HashTable<T>::member(T data, int key) {}

template <typename T> string HashTable<T>::to_string() {}
